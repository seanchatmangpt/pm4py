{
  "schema": "urn:pm4py-paas:operation-catalog:v1",
  "standing": "construct_only",
  "authority": "SELECT_CONSTRUCT_ONLY",
  "operations": [
{% for row in results %}    {"id":"{{ row.id }}","label":"{{ row.label }}","description":"{{ row.description }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
