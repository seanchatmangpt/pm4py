# PM4Py Secret for sensitive data
# NOTE: In production, use sealed-secrets or external secret management
apiVersion: v1
kind: Secret
metadata:
  name: pm4py-secret
  namespace: pm4py
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"  # Replace with actual password
  REDIS_PASSWORD: ""              # Empty if no auth
  OTEL_EXPORTER_OTLP_HEADERS: ""  # For authenticated OTEL endpoint
