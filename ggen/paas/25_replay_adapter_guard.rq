PREFIX paas: <https://ggen.dev/paas#>
SELECT ?receipt WHERE {
  ?receipt a paas:AnalysisReceipt ; paas:adapterIdentity ?adapter ; paas:replayAdapterIdentity ?replay .
  FILTER (?adapter != ?replay)
}
